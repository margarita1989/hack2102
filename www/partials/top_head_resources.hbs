<link rel="stylesheet" href="css/main.css"/>

<script>
    (function(global) {
        global.bookFace = {
            isLoggedIn: "{{ isLoggedIn }}",
            user: {
                name: "{{ user.google.name }}",
                books: "{{ user.books }}"
            }
        };

        bookFace.user.books = bookFace.user.books.split(',')
    })(window);
</script>

<script src="lib/require.js" data-main="require-config"></script>